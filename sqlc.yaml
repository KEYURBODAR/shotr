version: "2"
sql:
  - engine: "sqlite"
    schema: "migrations"
    queries: "db/queries"
    gen:
      go:
        package: "db"
        out: "db"
        emit_json_tags: true
        emit_prepared_queries: true